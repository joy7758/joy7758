# Profile Rewrite Plan

## Objective

Turn the profile repository into a single-theory-entry public surface for the five-layer verifiable autonomous systems stack.

## Planned Changes

1. Rewrite `README.md` around one canonical entry point: `digital-biosphere-architecture`.
2. Reframe the five core layer repositories in canonical order:
   - `persona-object-protocol`
   - `agent-intent-protocol`
   - `token-governor`
   - `fdo-kernel-mvk`
   - `aro-audit`
3. Separate adjacent repositories into `Supporting Annexes`.
4. Separate `verifiable-agent-demo` and `agent-governance-benchmark` into `Demo and Evaluation`.
5. Mark older DPP, sovereignty, and K-RFS repositories as `Legacy Lineage`.
6. Add a pinned-repository recommendation file that restores MVK into the six visible core slots.
7. Add a metadata-alignment TODO for repo descriptions and topics.
8. Update directly conflicting internal surface docs so they no longer contradict the README.

## Acceptance Criteria

- The front page has one canonical entry.
- The five-layer stack is visible in order.
- MVK is explicit on the front page.
- Demo and benchmark surfaces are not presented as primary theory entry points.
- Legacy repositories are preserved but clearly marked as historical.
