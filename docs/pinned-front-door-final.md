# Pinned Front Door Final

## Recommended pinned order

1. digital-biosphere-architecture
2. persona-object-protocol
3. verifiable-agent-demo
4. aro-audit

## Why this order works

- `#1` is the system view and canonical interpretation point.
- `#2` is the clearest concept entry into the architecture.
- `#3` is the smallest practical walkthrough across layers.
- `#4` is the strongest evidence-oriented rigor layer.

## What should not displace the top four right now

- `token-governor`
- `agent-object-protocol`

These repositories are important, but current front-door optimization should favor clarity over breadth.

## External citation campaign variant

Use this variant only when the profile is being optimized for external reuse,
community replies, or citation growth rather than architecture explanation.

1. verifiable-tool-invocation-flow
2. persona-object-protocol
3. verifiable-agent-demo
4. aro-audit

Why this variant works:

- `#1` is the smallest public receipt surface: one tool call, one signed receipt, one verifier.
- `#2` is the clearest persona/config reuse entry.
- `#3` gives a runnable before/after artifact.
- `#4` gives the deeper post-execution audit route.

Boundary: this is a routing recommendation only. It does not claim new posts,
external validation, package releases, or official framework integrations.

## Manual action reminder

Update pinned repositories in the GitHub UI to match this order.
