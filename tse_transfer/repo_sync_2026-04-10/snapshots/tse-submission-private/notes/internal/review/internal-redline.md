# Internal Redline

## Overclaim Fixes Made

- Kept the thesis fixed on one `operation accountability statement` and one bounded validation path throughout the manuscript.
- Rewrote the profile-boundary sentence in Section 3 so it no longer gestures toward a larger "governance surface" and instead stays with the narrower wording "general provenance model" and "platform architecture."
- Kept Section 5 anchored to the fresh clean rerun as the primary evaluation basis and removed repo-facing corroboration language from the evaluation ending.
- Kept related work as comparison context only; each subsection ends by restating that the paper is narrower than the adjacent line of work.
- Removed remaining repo-facing wording such as `handoff`, `artifact package`, and direct path-style phrasing from the main manuscript where those phrases made the draft read like project documentation.

## Stale Lines Removed

- No sentence remains saying the validator or demo were not rerun successfully.
- No scaffold markers such as `TODO`, `Purpose:`, or note-to-self text remain in `archive/legacy_drafts/manuscript/full-draft.md`.
- No legacy `Sovereign-pFDO` language or old figure-story language was carried into the full draft.
- The earlier risks tied to `FDO-based agent setting` wording and absolute local paths in the main manuscript have been resolved.

## Remaining Risky Wording

- The digital object and provenance comparisons in Related Work could invite readers to infer a larger architectural scope; the current contrast sentences are intended to prevent that drift.
- Terms such as `artifact`, `validation report`, and the CLI command are still intentionally concrete; if a venue strongly prefers less implementation-facing language in the main text, that change should be stylistic only and should not widen the claims.
