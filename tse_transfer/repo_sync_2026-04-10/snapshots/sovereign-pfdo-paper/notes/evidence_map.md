# Sovereign-pFDO Evidence Map

This file maps the current manuscript to the type of support each section may
carry.

Legend:

- `[P]` must have a primary or official source
- `[A]` may remain architecture-level only
- `[D]` should be deleted or further downgraded if it reappears
- `[M]` motivation/background only

## Abstract

- `[P]` `distributed digital-object environments` / `protocol-layer governance`
  background should be supported by `dona2018doip`, and optionally by
  `kahn2006framework` or `soilandreyes2024evaluating`.
- `[P]` `BLAKE3-based PID construction template` requires a primary BLAKE3
  source such as `aumasson2024blake3`.
- `[A]` `divergence-based gateway decision model` is architecture-level unless
  later backed by stronger operational evidence.
- `[A]` `offload-friendly deployment settings` is deployment consideration
  language only.
- `[D]` do not reintroduce `Digital Territory`, completed `1.6T` validation, or
  `real-time interception` as proven claims.

## 1. Introduction

- `[A]` `governance gap between normative layer and protocol execution`
  currently functions as architecture-level framing.
- `[A]` `protocol and architecture problem` is a framing statement, not a proof
  point.
- `[A]` `sovereignty-aware control` is the correct bounded phrase for the
  current line.
- `[D]` do not reintroduce `knowledge-sovereignty enforcement`.
- `[M]` high-throughput distributed environments are motivation, not a local
  performance result.

## 2. Problem Framing

- `[A]` `governance at the wrong layer` is a problem-definition statement.
- `[A]` `bounded architectural response` is a scope-control statement.
- `[M]` full sovereignty / full AI control / ultra-scale deployment may be
  mentioned only as pressures the paper is not claiming to solve.
- `[D]` do not let this section drift into completed infrastructure or
  validated-governance language.

## 3. Layered Architecture

- `[A]` the four-layer architecture is the manuscript's conceptual center and
  may remain architecture-level.
- `[P]` if specific ties to digital-object protocols or FDO interfaces are
  claimed, support them with `dona2018doip`, `kahn2006framework`, or
  `soilandreyes2024evaluating`.
- `[A]` this section should remain architecture-first, not experiment-first.

## 4. PID Construction Template

- `[P]` BLAKE3-related claims require `aumasson2024blake3`.
- `[A]` `hash-based identifier composition with low collision likelihood under
  standard cryptographic assumptions` is acceptable at current scope.
- `[A]` `node_identity` should be read as node-scoped identifier logic, not as
  an asserted hardware trust anchor.
- `[D]` do not drift back to `difficult-to-predict identifier generation` or
  replay/security claims beyond the current bounded wording.
- `[D]` do not turn the PID construction into authenticity or sovereignty proof.

## 5. Divergence-Triggered Gateway Intervention

- `[P]` the mathematical basis for KL divergence should be supported by
  `kullback1951information`.
- `[A]` `gateway-side discrepancy score` is acceptable as architecture-level
  wording.
- `[A]` `threshold-triggered escalation / policy review hook` is acceptable
  because it is bounded and non-empirical.
- `[A]` `P`, `Q`, and `tau` are now operationalized as task-specific and
  deployment-specific, which keeps the section defensible.
- `[D]` do not reintroduce validated-controller or universal-AI-governance
  language.

## 6. Offload-Oriented Deployment Considerations

- `[P]` `1.6 Tb/s` references must be grounded only in standards-trajectory
  sources such as `ieee8022025p8023dj`.
- `[P]` DPU / SmartNIC relevance must be grounded only in official product
  material such as `nvidiaBlueField3`.
- `[P]` CRC32c as a transport-integrity surface should be grounded in
  `stewart2022rfc9260`.
- `[A]` deployment relevance and offload orientation may remain architecture-
  level.
- `[D]` do not let this section sound like completed throughput proof.

## 7. Scope Boundary And Non-Goals

- `[A]` this section is a boundary-control section and may remain largely
  uncited.
- `[A]` the non-claims are currently aligned with the line identity:
  not a finished sovereign infrastructure, not PID-alone sovereignty, not a
  complete KL alignment theory, not full throughput proof, not a witness paper,
  not a verification-boundary paper.
- `[D]` do not weaken these non-goals in later revisions.

## 8. Conclusion

- `[A]` the conclusion should remain architecture-level and bounded.
- `[M]` computer-networks and distributed-governance relevance may be
  reiterated as positioning.
- `[D]` do not reintroduce victory-language such as `Digital Territory`,
  completed sovereignty proof, or completed terabit-scale validation.
